fallbacks:
	./build-abinit-fallbacks.sh

all: fallbacks

all_targets check clean default dist distcheck distclean distcleancheck install installcheck multi uninstall:
	@echo "Not doing anything for the fallbacks"

